import express from 'express';
import { z } from 'zod';
import { parseServiceRequest } from './domain/parser.js';
import { decideRequest } from './domain/decision.js';
import { GConnectBrowserProvider } from './integrations/gconnectBrowser.js';
import { GoogleRoutesClient, roundEtaToOperationalMinutes } from './integrations/googleRoutes.js';
import { WhatsAppCloudClient } from './integrations/whatsapp.js';

const app = express();
app.use(express.json());

const googleRoutes = new GoogleRoutesClient();
const whatsapp = new WhatsAppCloudClient();
const gconnect = new GConnectBrowserProvider();

const platformHtml = `<!doctype html>
<html lang="pt-BR">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Bot Guincho</title>
<style>
:root{--bg:#07111f;--panel:#0d1b2d;--panel2:#11243b;--line:#203651;--text:#f6fbff;--muted:#8fa6bf;--brand:#2ee59d;--danger:#ff6b6b;--warning:#ffd166}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 10% 0,#15314a 0,transparent 30%),var(--bg);color:var(--text);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.layout{min-height:100vh;display:grid;grid-template-columns:260px 1fr}.sidebar{border-right:1px solid var(--line);padding:28px 20px;background:#071525;position:sticky;top:0;height:100vh}.logo{display:flex;gap:12px;align-items:center;margin-bottom:30px}.logo-mark{width:42px;height:42px;border-radius:13px;background:var(--brand);display:grid;place-items:center;color:#042719;font-weight:950}.logo strong{display:block}.logo small,.muted{color:var(--muted)}nav{display:grid;gap:8px}.nav{border:0;text-align:left;color:var(--muted);background:transparent;padding:13px 14px;border-radius:12px;font-weight:800;cursor:pointer}.nav.active,.nav:hover{background:#122b47;color:#fff}.main{padding:38px;max-width:1320px;width:100%;margin:0 auto}.top{display:flex;justify-content:space-between;align-items:center;gap:20px;margin-bottom:28px}.top h1{margin:0;font-size:30px}.top p{margin:7px 0 0;color:var(--muted)}.status{display:flex;align-items:center;gap:9px;padding:10px 14px;border:1px solid var(--line);border-radius:999px;background:#0b1c2f}.dot{width:10px;height:10px;border-radius:50%;background:var(--warning)}.dot.ok{background:var(--brand)}.grid4{display:grid;grid-template-columns:repeat(4,1fr);gap:16px}.grid2{display:grid;grid-template-columns:1fr 1fr;gap:18px}.card{background:linear-gradient(180deg,var(--panel2),var(--panel));border:1px solid var(--line);border-radius:20px;padding:24px}.metric{font-size:30px;font-weight:900;margin:14px 0 6px}.section{margin-top:18px}.notice{padding:14px 16px;border-radius:13px;background:#172b40;color:var(--warning);margin-top:14px;line-height:1.5}.oknotice{color:#9ff5d0}.errornotice{color:#ffaaaa}.btn{border:0;border-radius:12px;padding:12px 16px;font-weight:850;cursor:pointer}.primary{background:var(--brand);color:#052719}.secondary{background:#17304c;color:white;border:1px solid var(--line)}.danger{background:#3a2028;color:#ffb1b1;border:1px solid #65313c}.actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:16px}.page{display:none}.page.active{display:block}.field{display:grid;gap:8px;margin:14px 0}.field input,.field textarea{width:100%;background:#081524;color:white;border:1px solid var(--line);border-radius:12px;padding:12px 13px;outline:none}.field textarea{min-height:180px;resize:vertical}.setup{display:grid;grid-template-columns:1fr 1fr;gap:12px}.qr{width:min(360px,80vw);background:white;border-radius:18px;padding:14px;margin:16px 0}.qr img{display:block;width:100%}.empty{padding:28px;border:1px dashed #31516f;border-radius:15px;color:var(--muted);text-align:center}.groups{display:grid;gap:10px;margin-top:16px}.group{display:flex;align-items:center;gap:12px;padding:14px;border:1px solid var(--line);border-radius:13px;background:#091929}.group input{width:18px;height:18px;accent-color:var(--brand)}.group strong{display:block}.group small{display:block;color:var(--muted);margin-top:3px}.switch{display:flex;justify-content:space-between;align-items:center;gap:20px;padding:14px 0;border-bottom:1px solid var(--line)}.switch input{width:20px;height:20px;accent-color:var(--brand)}.activity{display:grid;gap:10px}.event{padding:12px 14px;border:1px solid var(--line);border-radius:13px;background:#091929}.event small{color:var(--muted)}@media(max-width:900px){.layout{grid-template-columns:1fr}.sidebar{position:static;height:auto;border-right:0;border-bottom:1px solid var(--line)}nav{grid-template-columns:repeat(4,1fr)}.main{padding:22px 16px}.grid4,.grid2,.setup{grid-template-columns:1fr 1fr}}@media(max-width:600px){.grid4,.grid2,.setup{grid-template-columns:1fr}.top{align-items:flex-start;flex-direction:column}nav{grid-template-columns:1fr 1fr}}
</style>
</head>
<body>
<div class="layout">
<aside class="sidebar"><div class="logo"><div class="logo-mark">BG</div><div><strong>Bot Guincho</strong><small>Central operacional</small></div></div><nav><button class="nav active" data-page="overview">Visão geral</button><button class="nav" data-page="whatsapp">WhatsApp</button><button class="nav" data-page="groups">Grupos</button><button class="nav" data-page="ai">Inteligência</button></nav></aside>
<main class="main">
<div class="top"><div><h1 id="title">Central operacional</h1><p id="subtitle">Configure e acompanhe a automação do atendimento.</p></div><div class="status"><span id="statusDot" class="dot"></span><span id="statusText">Plataforma online</span></div></div>

<section id="overview" class="page active">
<div class="grid4"><div class="card"><b>Plataforma</b><div class="metric">Online</div><div class="muted">Vercel + API</div></div><div class="card"><b>WhatsApp</b><div class="metric" id="mWa">—</div><div class="muted">Sessão persistente</div></div><div class="card"><b>Grupos</b><div class="metric" id="mGroups">—</div><div class="muted">Selecionados</div></div><div class="card"><b>IA</b><div class="metric" id="mAi">—</div><div class="muted">Resposta automática</div></div></div>
<div class="grid2 section"><div class="card"><h2>Conexão do worker</h2><p class="muted">O WhatsApp roda em um servidor persistente. Informe a URL segura do worker uma única vez neste navegador.</p><div class="setup"><div class="field"><label>Worker URL</label><input id="workerUrl" placeholder="https://...trycloudflare.com"></div><div class="field"><label>Token</label><input id="workerToken" type="password" placeholder="Token do worker"></div></div><div class="actions"><button class="btn primary" id="saveWorker">Conectar worker</button><button class="btn secondary" id="testWorker">Testar conexão</button><button class="btn danger" id="clearWorker">Desconectar</button></div><div id="workerNotice" class="notice">Aguardando configuração do worker.</div></div><div class="card"><h2>Atividade recente</h2><p class="muted">Mensagens, respostas e eventos da sessão.</p><div id="activity" class="activity section"></div></div></div>
</section>

<section id="whatsapp" class="page"><div class="card"><h2>WhatsApp Web</h2><p class="muted">Escaneie o QR pelo WhatsApp em Configurações → Aparelhos conectados → Conectar um aparelho.</p><div id="qrBox" class="empty section">Conecte o worker para carregar o QR Code.</div><div id="waNotice" class="notice">Aguardando worker.</div><div class="actions"><button class="btn secondary" id="refreshWa">Atualizar status</button></div></div></section>

<section id="groups" class="page"><div class="card"><h2>Grupos monitorados</h2><p class="muted">O bot só responde nos grupos marcados abaixo.</p><div id="groupList" class="groups"><div class="empty">Conecte o WhatsApp para listar os grupos.</div></div><div class="actions"><button class="btn primary" id="saveGroups">Salvar grupos</button><button class="btn secondary" id="refreshGroups">Sincronizar grupos</button></div></div></section>

<section id="ai" class="page"><div class="grid2"><div class="card"><h2>Inteligência artificial</h2><p class="muted">Configure como o bot interpreta e responde às mensagens dos grupos autorizados.</p><div class="field"><label>Modelo</label><input id="aiModel" placeholder="gpt-5-mini"></div><div class="field"><label>Instruções do atendente</label><textarea id="aiInstructions"></textarea></div><div class="actions"><button class="btn primary" id="saveAi">Salvar configuração</button></div><div id="aiNotice" class="notice">Aguardando worker.</div></div><div class="card"><h2>Automação</h2><div class="switch"><div><strong>IA ativa</strong><div class="muted">Responder automaticamente nos grupos selecionados.</div></div><input id="aiEnabled" type="checkbox"></div><div class="switch"><div><strong>Responder toda mensagem</strong><div class="muted">Processar cada nova mensagem recebida.</div></div><input id="replyEvery" type="checkbox"></div><div class="switch"><div><strong>Modo humano</strong><div class="muted">Pausa a resposta automática sem desconectar.</div></div><input id="humanTakeover" type="checkbox"></div></div></div></section>
</main></div>
<script>
const titles={overview:['Central operacional','Configure e acompanhe a automação do atendimento.'],whatsapp:['WhatsApp','Conecte e acompanhe a sessão do cliente.'],groups:['Grupos','Escolha exatamente onde o bot pode atuar.'],ai:['Inteligência','Configure o comportamento do atendente automático.']};
document.querySelectorAll('.nav').forEach(btn=>btn.onclick=()=>{document.querySelectorAll('.nav').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.page').forEach(x=>x.classList.remove('active'));btn.classList.add('active');document.getElementById(btn.dataset.page).classList.add('active');document.getElementById('title').textContent=titles[btn.dataset.page][0];document.getElementById('subtitle').textContent=titles[btn.dataset.page][1]});
const $=id=>document.getElementById(id);let settings={};
function getWorker(){return{url:(localStorage.getItem('botguincho_worker_url')||'').replace(/\/$/,''),token:localStorage.getItem('botguincho_worker_token')||''}}
$('workerUrl').value=getWorker().url;$('workerToken').value=getWorker().token;
async function api(path,opt={}){const w=getWorker();if(!w.url)throw new Error('Worker não configurado');const headers={...(opt.headers||{}),'x-botguincho-token':w.token};if(opt.body&&!headers['content-type'])headers['content-type']='application/json';const r=await fetch(w.url+path,{...opt,headers,cache:'no-store'});let d;try{d=await r.json()}catch{d={}}if(!r.ok)throw new Error(d.message||d.error||('HTTP '+r.status));return d}
function esc(v){const d=document.createElement('div');d.textContent=v||'';return d.innerHTML}
function setNotice(id,text,kind=''){const el=$(id);el.textContent=text;el.className='notice '+kind}
async function loadStatus(){try{const s=await api('/api/status');const ready=s.whatsapp?.status==='pronto';$('statusDot').className='dot '+(ready?'ok':'');$('statusText').textContent=ready?'WhatsApp conectado':'Plataforma online';$('mWa').textContent=ready?'Online':(s.whatsapp?.status||'Offline');$('mGroups').textContent=s.groupsSelected??0;$('mAi').textContent=s.ai?.enabled?'Ativa':'Pausada';if(s.whatsapp?.qrDataUrl){$('qrBox').className='section';$('qrBox').innerHTML='<div class="qr"><img src="'+s.whatsapp.qrDataUrl+'"></div>'}else{$('qrBox').className='empty section';$('qrBox').innerHTML=ready?'WhatsApp conectado com sucesso.':'Aguardando geração do QR Code...'}setNotice('workerNotice','Worker conectado com sucesso.','oknotice');setNotice('waNotice',ready?'WhatsApp conectado e pronto.':'Estado da sessão: '+(s.whatsapp?.status||'desconhecido'),'oknotice');return s}catch(e){$('mWa').textContent='Offline';$('mGroups').textContent='—';$('mAi').textContent='—';setNotice('workerNotice','Worker desconectado: '+e.message,'errornotice');setNotice('waNotice','Não foi possível alcançar o worker.','errornotice');throw e}}
async function loadGroups(){try{const d=await api('/api/groups');$('groupList').innerHTML=d.groups?.length?d.groups.map(g=>'<label class="group"><input type="checkbox" value="'+esc(g.id)+'" '+(g.selected?'checked':'')+'><div><strong>'+esc(g.name)+'</strong><small>'+esc(g.id)+'</small></div></label>').join(''):'<div class="empty">Nenhum grupo identificado. Envie uma mensagem em um grupo e clique em Sincronizar grupos.</div>'}catch(e){$('groupList').innerHTML='<div class="empty">'+esc(e.message)+'</div>'}}
async function loadSettings(){try{settings=await api('/api/settings');$('aiEnabled').checked=!!settings.aiEnabled;$('replyEvery').checked=!!settings.replyEveryMessage;$('humanTakeover').checked=!!settings.humanTakeover;$('aiModel').value=settings.aiModel||'';$('aiInstructions').value=settings.aiInstructions||'';setNotice('aiNotice',settings.apiKeyConfigured?'OpenAI configurada no worker.':'A IA ainda precisa da OPENAI_API_KEY no worker.',settings.apiKeyConfigured?'oknotice':'')}catch(e){setNotice('aiNotice','Não foi possível carregar a IA: '+e.message,'errornotice')}}
async function loadActivity(){try{const d=await api('/api/activity');$('activity').innerHTML=d.activity?.length?d.activity.slice(0,10).map(e=>'<div class="event"><strong>'+esc(e.message)+'</strong><br><small>'+new Date(e.at).toLocaleString('pt-BR')+' · '+esc(e.type)+'</small></div>').join(''):'<div class="empty">Ainda não há atividade.</div>'}catch{$('activity').innerHTML='<div class="empty">Worker ainda não conectado.</div>'}}
async function refreshAll(){await loadStatus();await Promise.all([loadGroups(),loadSettings(),loadActivity()])}
$('saveWorker').onclick=async()=>{localStorage.setItem('botguincho_worker_url',$('workerUrl').value.trim());localStorage.setItem('botguincho_worker_token',$('workerToken').value.trim());try{await refreshAll()}catch{}};
$('testWorker').onclick=async()=>{try{await loadStatus()}catch{}};
$('clearWorker').onclick=()=>{localStorage.removeItem('botguincho_worker_url');localStorage.removeItem('botguincho_worker_token');location.reload()};
$('refreshWa').onclick=()=>loadStatus().catch(()=>{});$('refreshGroups').onclick=loadGroups;
$('saveGroups').onclick=async()=>{try{const groupIds=[...document.querySelectorAll('#groupList input:checked')].map(x=>x.value);await api('/api/groups',{method:'POST',body:JSON.stringify({groupIds})});await Promise.all([loadGroups(),loadStatus()]);alert('Grupos salvos.')}catch(e){alert(e.message)}};
$('saveAi').onclick=async()=>{try{await api('/api/settings',{method:'POST',body:JSON.stringify({aiEnabled:$('aiEnabled').checked,replyEveryMessage:$('replyEvery').checked,humanTakeover:$('humanTakeover').checked,aiModel:$('aiModel').value,aiInstructions:$('aiInstructions').value})});await Promise.all([loadSettings(),loadStatus()]);alert('Configuração salva.')}catch(e){alert(e.message)}};
['aiEnabled','replyEvery','humanTakeover'].forEach(id=>$(id).onchange=async()=>{try{await api('/api/settings',{method:'POST',body:JSON.stringify({aiEnabled:$('aiEnabled').checked,replyEveryMessage:$('replyEvery').checked,humanTakeover:$('humanTakeover').checked,aiModel:$('aiModel').value,aiInstructions:$('aiInstructions').value})});await loadStatus()}catch(e){alert(e.message)}});
(async()=>{try{await checkPlatform();if(getWorker().url)await refreshAll();else{setNotice('workerNotice','Informe a URL e o token do worker para ativar WhatsApp, grupos e IA.');loadActivity()}}catch{}})();
async function checkPlatform(){try{const r=await fetch('/health',{cache:'no-store'});const d=await r.json();$('statusDot').className='dot '+(d.ok?'ok':'');$('statusText').textContent=d.ok?'Plataforma online':'Falha na plataforma'}catch{$('statusText').textContent='API indisponível'}}
setInterval(()=>{if(getWorker().url){loadStatus().catch(()=>{});loadActivity().catch(()=>{})}},7000);
</script>
</body></html>`;

app.get('/', (_req, res) => res.type('html').send(platformHtml));
app.get('/health', (_req, res) => res.json({ ok: true, service: 'botguincho', integrations: { googleRoutes: googleRoutes.isConfigured(), whatsapp: whatsapp.isConfigured(), whatsappSendEnabled: whatsapp.isSendEnabled(), gconnect: gconnect.isConfigured() } }));

const parseBodySchema = z.object({ text: z.string().min(1), insurer: z.string().optional() });
app.post('/api/requests/parse', (req, res) => { const parsedBody = parseBodySchema.safeParse(req.body); if (!parsedBody.success) return res.status(400).json({ error: 'invalid_request', details: parsedBody.error.flatten() }); const request = parseServiceRequest(parsedBody.data.text, parsedBody.data.insurer); return res.json({ request, decision: decideRequest(request) }); });

const etaTestSchema = z.object({ origin: z.object({ latitude: z.number().min(-90).max(90), longitude: z.number().min(-180).max(180) }), destinationAddress: z.string().min(3), roundToMinutes: z.number().int().positive().max(60).default(10) });
app.post('/api/eta/test', async (req, res) => { const p=etaTestSchema.safeParse(req.body); if(!p.success)return res.status(400).json({error:'invalid_request',details:p.error.flatten()}); try{const route=await googleRoutes.computeEta({origin:p.data.origin,destinationAddress:p.data.destinationAddress});const suggestedMinutes=roundEtaToOperationalMinutes(route.durationSeconds,p.data.roundToMinutes);return res.json({route,distanceKm:Number((route.distanceMeters/1000).toFixed(1)),rawEtaMinutes:Number((route.durationSeconds/60).toFixed(1)),suggestedEtaMinutes:suggestedMinutes,suggestedReply:`${suggestedMinutes} minutos ou menos`})}catch(error){return res.status(502).json({error:'route_error',message:error instanceof Error?error.message:'Erro desconhecido ao consultar Google Routes.'})} });

const gconnectTestSchema=z.object({vehicleId:z.string().min(1).optional()});
app.post('/api/gconnect/position',async(req,res)=>{const p=gconnectTestSchema.safeParse(req.body??{});if(!p.success)return res.status(400).json({error:'invalid_request',details:p.error.flatten()});const vehicleId=p.data.vehicleId??process.env.GCONNECT_DEFAULT_VEHICLE??'GSWOH17';try{return res.json({vehicleId,position:await gconnect.getCurrentPosition(vehicleId)})}catch(error){return res.status(502).json({error:'gconnect_error',message:error instanceof Error?error.message:'Erro desconhecido ao consultar GConnect.'})}});

const liveEtaSchema=z.object({vehicleId:z.string().min(1).optional(),destinationAddress:z.string().min(3),roundToMinutes:z.number().int().positive().max(60).default(10)});
app.post('/api/eta/live',async(req,res)=>{const p=liveEtaSchema.safeParse(req.body);if(!p.success)return res.status(400).json({error:'invalid_request',details:p.error.flatten()});const vehicleId=p.data.vehicleId??process.env.GCONNECT_DEFAULT_VEHICLE??'GSWOH17';try{const position=await gconnect.getCurrentPosition(vehicleId);const route=await googleRoutes.computeEta({origin:position,destinationAddress:p.data.destinationAddress});const suggestedMinutes=roundEtaToOperationalMinutes(route.durationSeconds,p.data.roundToMinutes);return res.json({vehicleId,position,route,distanceKm:Number((route.distanceMeters/1000).toFixed(1)),rawEtaMinutes:Number((route.durationSeconds/60).toFixed(1)),suggestedEtaMinutes:suggestedMinutes,suggestedReply:`${suggestedMinutes} minutos ou menos`})}catch(error){return res.status(502).json({error:'live_eta_error',message:error instanceof Error?error.message:'Erro ao consultar posição e ETA.'})}});

const port=Number(process.env.PORT??3000);
app.listen(port,'0.0.0.0',()=>console.log(`botguincho listening on port ${port}`));
