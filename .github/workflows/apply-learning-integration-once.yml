name: Apply learning integration once
on:
  workflow_dispatch:
  push:
    paths:
      - 'tools/apply_learning_integration_tmp.py'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python tools/apply_learning_integration_tmp.py
      - run: node --check tools/vercel-whatsapp-worker.mjs
      - run: node --check tools/learning-engine.mjs
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tools/vercel-whatsapp-worker.mjs
          git diff --cached --quiet && exit 0
          git commit -m "Integra conhecimento contínuo por grupo"
          git push
