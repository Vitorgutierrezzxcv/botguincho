const $=id=>document.getElementById(id);const companyId=(new URLSearchParams(location.search).get('companyId')||localStorage.getItem('bg-company-id')||'').toLowerCase().replace(/[^a-z0-9-]/g,'-').replace(/-+/g,'-').replace(/^-|-$/g,'').slice(0,42);if(companyId)localStorage.setItem('bg-company-id',companyId);function show(msg,bad=true){$('notice').style.display='block';$('notice').className='notice section '+(bad?'bad':'good');$('notice').textContent=msg}async function login(){try{$('login').disabled=true;const r=await fetch('/api/control/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:$('email').value,password:$('password').value})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'login_failed');localStorage.setItem('bg-access-token',d.access_token);const me=await fetch('/api/control/me',{headers:{authorization:`Bearer ${d.access_token}`}});const m=await me.json().catch(()=>({}));if(!me.ok)throw new Error(m.error||'session_failed');const master=!!m.master;const allowed=master||(m.memberships||[]).some(x=>x.companies?.slug===companyId);if(!allowed){localStorage.removeItem('bg-access-token');throw new Error('forbidden')}location.replace('/?companyId='+encodeURIComponent(companyId))}catch(e){show(e.message==='forbidden'?'Seu usuário não possui acesso a esta empresa.':'Não foi possível entrar. Confira e-mail e senha.')}finally{$('login').disabled=false}}$('login').onclick=login;$('password').addEventListener('keydown',e=>{if(e.key==='Enter')login()});